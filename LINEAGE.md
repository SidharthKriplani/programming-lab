# PL — LINEAGE

_Narrative history of Programming Lab. One line per meaningful turn._

- **2026-06-23 — Re-scoped from Production Systems Lab to Programming Lab.** The repo began (Dec 2024) as an infra-ops teaching lab: FastAPI, Docker, async APIs, databases, caching, scaling, observability. That scope was superseded by HQ D-07 (PSL = the SWE layer for data people: DSA, Python, pandas; glass-box "feel the machine") and D-15 (the fluency rung of the Competence Model). HQ authored `docs/PL-BUILD-SPEC.md` (the four fluency banks, B-ready schema, Pyodide IDE plan). Sidharth approved Option A (rebuild as a React+Vite+Pyodide SPA, archive the legacy infra) and the rename PSL → PL (Programming Lab); the Vercel-slug rename was deferred.
- **2026-06-23 — B0 + B1 built.** Legacy infra archived to `_legacy/`. New sibling-consistent SPA stood up with a glass-box Pyodide IDE (CodeMirror + real CPython + time/memory instrumentation, built early as the differentiator, not last). Bank A shipped: 23 Python gotchas, each a runnable predict→reveal→fix loop that doubles as a LinkedIn post — the distribution-gate override is honored because the content *is* the distribution. B2 (pandas/numpy) is the next bank.
- **2026-06-23 — Built, slug-renamed, shipped to GitHub.** `npm run build` passed on macOS (vite 8.0.16, built in 217ms). The GitHub repo was renamed `production-systems-lab` → **`programming-lab`** (slug rename pulled forward from "deferred"); the local remote was repointed and `a7677fc` pushed. Vercel import (first deploy) is the only remaining step. Local folder kept as `labs/production-systems-lab` to avoid churning the mount + CLAUDE.md paths.
- **2026-06-23 — Got its own identity.** Shipped to Vercel (`programming-lab.vercel.app`) wearing PAL's exact light/indigo theme, which read as "another PAL". Researched the BreakLabs "Glass Box" brand and found PL is the lab the brand was describing (the dark instrument that exposes the machine) and that PL's assigned wayfinding colour is violet `#8B5CF6`. Proposed three directions; Sidharth picked **Instrument** (dark-first, violet/void, Inter + JetBrains Mono) with a light "Field Notes" toggle and the break-glyph favicon/wordmark. Implemented as a pure token swap — no component changes.
- **2026-06-23 — BreakLabs logo (D-19).** HQ shipped `BRANDMARK-ROLLOUT.md` (the unified `break⌇labs` lockup — one brand, distinct rooms; net-new, no lab rendered the wordmark before). PL built the `BrandMark` component (descriptor `Programming`, violet accent, red seam + wordmark constant) and wired it into the 7 standard slots, including a rebranded favicon (shared red monogram) and a new OG share card. PL is the first lab to implement it; PAL/MSL/GAL still pending.
- **2026-06-23 — Sidebar brought to the HQ standard.** HQ extracted a canonical Sidebar Standard (four-frame accordion, one-open-per-level, measured-height motion, frame icons, active pill, SOON badges — the last being PL's own contribution). PL's nav had been static (frames always expanded); rebuilt it to the accordion spec with module-scoped `Collapsible`/`Chevron`/`NavItem` and follows-navigation auto-expand. `⌘K` search deferred (one live tab).
- **2026-06-23 — First real coding content beyond gotchas.** Researched the interview canon (NeetCode/Blind-75 patterns, pandas analyst ops) + Pyodide package loading, then built a test-based problem runner and authored Bank C (6 Python drills) + Bank D (5 pandas) — original, analyst-framed, all verified in CPython/pandas (38/38). The DO rung now has three live banks; pandas runs in-browser via an on-demand wheel load.
- **2026-06-23 — Banks taken to interview scope.** Researched what senior SWE/DS/MLE/DA/BA/PA/AIE are expected to code in Python + pandas, then expanded the Python drills to 56 (11 patterns, incl. a numpy-ml cluster for DS/MLE) and pandas to 41 (10 patterns) — via two focused build agents, each self-verifying. 120 problems total; all 337 checks independently re-verified out of the built JS in CPython/pandas.
- **2026-06-24 — KNOW re-scoped into the Foundations rooms (PAL handoff answered).** PAL sent a mentor note (`docs/FOUNDATIONS-HANDOFF.md`): *teach by manipulation, not exposition* — and PL's KNOW frame, though "shipped," is a 20-card predict-run-read stub. A live read of PAL's deployed lab (open a Metrics module → a three-slider Mix-Shift simulator) plus a grep (PAL = 236 files / 128k lines / 23 slider-driven files vs PL's 49 / 13k / 0) settled that PAL is the densest sibling and the critique lands. Pushed back, researched the canon (roadmap.sh, USACO, OSSU, PyTorch/NumPy docs, Python Tutor/VisuAlgo), and architected PL's answer: KNOW becomes a **trunk (5 rooms) + branches (2)** Foundations system, every module a manipulable model (preferring Pyodide-`live`, PL's edge over PAL's hand-built SVG). Two branches (Competitive Programming, Tensors & Autograd) consciously amend the easy→med / no-ML-internals charter — recorded as **D-PL-21**, not smuggled. Shipped the **skeleton** only: `docs/FOUNDATIONS-SPEC.md` (authority) + `src/data/foundationsRooms.js` (7 rooms / 24 clusters / 73 seed modules, `planned`, unimported). Build starts at F0 (the `interactive` slot + one `live` aliasing module as proof). The lab's lesson this turn: "the KNOW frame exists" ≠ "KNOW is built."
- **2026-06-23 — Adopted PAL's SQL-Lab rigor.** Studied (via subagent) how PAL's SQL Lab is built — shared datamarts, solution-computed expected output, layered tolerant comparison, a committed Tier-1/Tier-2 audit, and the hints/debrief/forensic/`beforeWriting` schema. Inherited the content *architecture*: shipped a committed audit gate (`scripts/audit_problems.py` — 120 problems / 0 Tier-1) + `docs/CONTENT-STANDARD.md`. Left the SQL-specific substrate behind (PL runs Pyodide + pandas, not sql.js; pandas-native NaN-aware comparison; an AST sandbox instead of a keyword ban). Shared DataFrame fixtures, the forensic/judgment format, and hints/debrief enrichment are the sequenced next passes.
- **2026-06-24 — Foundations built out: skeleton → all 7 rooms have live models.** Took the D-PL-21 skeleton all the way. F0: wired `FoundationsBrowser` (registry → trunk/branches → rooms → modules) into the KNOW nav. Then merged the 20 "Python & OOP Depth" modules INTO Foundations (one KNOW surface, mirroring the PyLab DO consolidation) — `KNOW_BACKING`/`KNOW_EXTRA`. Built the `interactive` slot in `KnowRunner` and authored driven models room by room: aliasing → copy-vs-view → call-stack → Big-O → hashing → vectorized race → broadcasting → index-align → truthiness → decorators → generators → async → the DSA traces (two-pointer/window/binary-search) → DP grid → autograd. When Sidharth pushed back ("your interactiveness means code, no" — it doesn't scale to 73), extracted **two config-driven templates** so the repeating shapes are DATA, not components: `StateTrace` (binding/identity) + `ArrayTrace` (DSA array traces), recorded as **D-PL-24**. Ended at **19 driven models, all 7 rooms covered, 7 of them config-driven**; every live model runs real CPython/pandas/numpy in Pyodide (PL's edge over PAL's hand-built SVG), all CPython/esbuild-verified, deployed incrementally. KNOW went from a 20-card stub to the densest interactive surface in the lab. The turn's lesson: build a few bespoke, see the shape, then templatize — the same way StateTrace emerged.
- **2026-06-24 — Foundations took SQL Lab's active loop (the pedagogy, not the chrome).** Sidharth asked what KNOW could learn from PAL's polished SQL Lab. The honest answer: not the solve-furniture (schema browser, company tags, two-pane — Foundations is a *learn* frame, not a problem bank), but the **active do-and-check loop** — produce an answer and get *targeted* feedback, instead of just watching a demo. Added an optional `yourTurn` step to `KnowRunner` (`runCheck` in pyodideRuntime runs the learner's code + a check in one fresh namespace → pass/fail + an actionable message, never just "wrong") and rolled it across **8 read-run modules** (closure, aliasing, or-default, mutable-default, eq/hash, wraps, iterator, operator dispatch), all CPython-verified. The editor half (names-only autocomplete + Cmd/Ctrl+Enter) came free from the parallel PyLab editor work (D-PL-26) — reused, not rebuilt. Recorded as **D-PL-27**: a new such challenge is now pure data (`yourTurn` in `knowModules.js`), no engineering.
- **2026-06-25 — Content batch 1 authored (ramp 1-5, pandas/numpy world).** First five problems in the concept ladder: single-col mean → boolean filter → NaN-aware filter → groupby+mean → groupby+NaN-group. Each adds exactly one new concept. Engineered fixtures so traps fire precisely (bo=80 for the > vs >= boundary; dan has valid score but NaN dept for the dropna trap; NaN dept row for the dropna=False groupby trap). All solutions and traps CPython-verified before transcribing — divergence confirmed on every trap. `pyLabBatch_ramp1.js` + 4 fixtures; wired into pyLabProblems + pyLabFixtures; 141 problems, 0 T1, 0 T2 audit clean. Push pending with 0.38-0.39. Batch 2 (ramp 6-10: multi-agg, simple merge, merge+duplicates, left merge, pivot_table) is next.
- **2026-06-25 — Learning path skeletons (0.42.0).** `src/data/pyLabPaths.js` — two structured tracks per scope: `3day` (intermediate, covers the core patterns a DA/DS/PA take-home tests) and `7day` (advanced, exits at senior-interview readiness). Two scopes: per-world (7 worlds × 2 tiers = 14 path objects with day-by-day titles, focus descriptions, and conceptSlugs) and lab-wide (2 cross-world paths). All `problemIds` arrays empty — content fills as the problem bank grows past ramp batch 2. File is unimported and unwired; no build impact. Brace diff 0, no backticks.
- **2026-06-25 — SQL Lab parity layout + contrast (0.41.0).** PyLabRunner had a structural gap vs SQL Lab: the `Reveal solution` button and the entire revealed section (model solution, debrief, JudgmentLayer, ScaleRace, RefactorChallenge, FollowUpChain, ForwardPointerCard) sat OUTSIDE `pylab-solve-grid`, rendering full-width below it and collapsing the two-column split at the exact moment the screen gets densest. Fix: all of that content moved into the RIGHT column. Left column (prompt + beforeWriting + ambiguity + schema) is now permanently fixed for the full solve cycle — same contract as SQL Lab. Second fix: `--text-secondary/muted/dim` were too subtle in all three theme blocks. Bumped the whole cascade: light-root secondary #4A4036→#2A1C10, muted #756B60→#4A4036, dim #9B8E80→#756B60; dark secondary #E2D4C2→#EFE7D5, muted #B4A693→#D4C8B8, dim #8C7B6A→#A89888; explicit-light same as root. Debrief text changed from `var(--text-secondary)` to `var(--text)` — it's long-form reading content, not metadata.
- **2026-06-25 — Icon system unified + Worlds/Gates skeleton shipped.** Two passes this session. First: merged HQ's 84-icon SVG superset into PL's `Icon.jsx` (+ 4 PL-specific additions: cpu/copy/play/clipboard-check + GLYPH_TO_ICON lookup map); copied `CompanyLogo.jsx` + `companyDomains.js` (319 curated mappings + favicon fallback) from HQ and wired CompanyLogo into the PyLab problem cards replacing the colored-initial-badge; swept all 6 emoji/glyph occurrences in PL src (PythonCell, MockLoop, BroadcastModel, TruthinessModel, BuildBrowser) to monochrome Icon components — the visual register is now fully within the HQ system. Second: built the Worlds + Gates skeleton — a 7-world tab layer (`WorldTabs`) over PyLabBrowser where each world is a topic-set lens with two unlock paths: self-declare (→ Correctness) or quiz-verified (→ Correctness or Judgment by score); the gate state persists in localStorage (`pl-world-gates-v1`). Five new files: `pyLabWorlds.js` (world defs), `worldGates.js` (unlock/persist/reset), `WorldTabs.jsx`, `WorldGate.jsx`, `GateQuiz.jsx`; PyLabBrowser fully wired. GateQuiz gracefully falls back when a world has < 3 MCQs — which is most of the bank right now. That's the honest content gap: the skeleton is sound but needs content. Agreed on the filling strategy: concept-accumulation ramp (each problem adds exactly one new idea, not abstract difficulty), batch 1 = first 5 concepts in the pandas/numpy world (single-col agg → row filter → filter+NaN → groupby → groupby+NaN), then scaffold 400–600 stubs across all worlds for batch-filling over time. The session's lesson: the gate system is the incentive structure; the MCQs are what make it real. Push pending (0.38.0 + 0.39.0) — needs macOS build first.
- **2026-06-24 — PyLab got a beginner on-ramp (the SQLBolt-style tutorial ladder).** Sidharth asked for "SQLBolt but for Python/pandas — completely beginner ramping to easy/med/hard." Brainstormed first (no code): the honest read is the gap is real (PyLab the gym assumes fluency; Foundations teaches *mental models* — neither is a write-your-first-line-and-check ramp), but a **standalone** SQLBolt clone would compete on the incumbents' turf and dilute the "runs vs right" judgment moat. Sidharth's call: make it an **offering inside PyLab** — the exact precedent PAL's SQL Lab already ships (a guided-walkthrough banner atop the bank) — so PL keeps its gym identity and no beginner bounces. Sized it honestly: bound the ladder to the gym's *Easy floor*, not "all of Python" (~18 Python lessons + an 8-lesson pandas section, one continuous track with skip-ahead). Built the **skeleton**: `src/data/pyTutorial.js` (Python lessons 1–5 fully authored — 20 inline tasks, every `solution`+`check` CPython-verified out of the written file: correct passes, starter fails, no errors; lessons 6–18 + the pandas section as planned stubs), `src/pages/PyTutorial.jsx` (the lesson player, reusing `PythonCell` + `runCheck` — the D-PL-27 contract), a "Start from zero" banner + internal `tutorial` view in `PyLabBrowser` (like Mock interview, no route/nav change), and `docs/PYLAB-TUTORIAL-SPEC.md`. Ramps **0 → Easy** then hands to the gym (`graduatesTo`); the gym takes Easy → Staff. Recorded as **D-PL-28**. Build green (vite 8, 109 modules). Shipped **0.37.0**; the push needed a rebase — `git add -A` had swept in the pending 0.33–0.36 stack, whose CHANGELOG edits collided with upstream (resolved: tutorial = 0.37.0, the eval-rubric doc demoted to 0.36.1). The turn's lesson: the ladder is the *welcome mat*, the gym is the *moat* — author the mat, but the depth is what makes it amazing.
