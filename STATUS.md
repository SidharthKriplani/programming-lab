# PL — STATUS

_Last updated: 2026-07-03 (session close). **0.38.0–0.42.0 shipped and live.** 0.43.0 (come-alive Track-1) built locally, ready for review._

**0.43.0 — come-alive Track 1 + Data-craft world (D-PL-29), local:**
- Track 1: `DebriefBlocks` wired into the PyLab reveal (labeled debrief grammar); `pyLabPaths.js` problemIds populated for python-core/pandas-numpy/oop-design + lab-wide (validated); empty world tabs hidden (render only worlds with real/planned content); path-open resets the level filter.
- Data-craft world (Track-2 #1): new `pyLabBatch_datacraft.js` — **10 analyst-judgment problems** (the right denominator, dedup-latest, NaN-is-not-zero, median-vs-mean, weighted rate, Simpson\'s paradox, funnel step conversion, week-1 retention, safe divide-by-zero, text-to-number coercion). Topic `data-craft` registered (PYLAB_TOPICS/ORDER, pyLabMeta roles PA/BA/DA/DS/MLE/AIE, data-craft paths). Every solution + honest method + trap CPython-verified; authored to `docs/PYLAB-CONTENT-RUBRIC.md` (labeled debriefs, trap runs-and-diverges). **Bank now 151 problems; audit 0 T1 / 0 T2, verify 0 fails, content-scan 0.** Also fixed a pre-existing broken trap (`pylab-col-mean`: fx_scores now has 2 numeric columns so the df.mean() Series trap actually diverges).
- Rubric added: `docs/PYLAB-CONTENT-RUBRIC.md` — the two-lens bar (SQL-Lab pedagogy parity + interview-competence) every problem clears.
- Track-2 backlog added: `docs/PYLAB-TRACK2-BACKLOG.md` — the full content universe by world × difficulty ladder × role (data-structures + data-manipulation enumerated, senior/advanced tier flagged as the gap).
- **Ladder batch 1** (`pyLabBatch_ladder1.js`): 9 problems — Python data-structure manipulation (dedup-order, invert-multimap, group-consecutive-runs, top-k-frequent) + senior/advanced data manipulation (merge fan-out, chained-assignment, a multi-step net-revenue pipeline) + ML-from-scratch (F1, z-score). **First 2 `stretch` (systems-tier) problems in the whole bank** (merge fan-out, net-revenue pipeline). All CPython-verified, wired.
- **DSA taxonomy fix:** re-tagged the 45 algorithmic drills (drills1/2/3) from topic `python-core` to a new `dsa` topic — lighting up the **DSA world with 50 problems** (hashing, two-pointer, sliding window, binary search, heap, stack, intervals, greedy/DP) + curated 3-day/7-day DSA paths. Near-zero authoring. python-core world now = idioms + data-structure manipulation; `group-names-by-team` kept as core. `dsa` registered (TOPICS/ORDER, meta roles SWE/MLE/AIE/DS). Python-core + lab paths cleaned of the moved ids.
- **Worlds now: 5 of 7 populated** (python-core 25, pandas-numpy 59, dsa-patterns 50, oop-design 15, data-craft 11; python-internals + code-craft still empty → hidden). **Bank 160 problems; all gates green (0 T1 / 0 T2 / verify 0 / scan 0); all path IDs valid.**
Next (Track 2, from backlog): ML-from-scratch + AI-engineering world; more senior/advanced (stretch) tier; python-internals + code-craft; data-craft v2.

**Current version: PL 0.42.0 (live)**
**Live on Vercel: PL 0.42.0** (programming-lab.vercel.app — tutorial ladder, two-pane solve, Graphite, Worlds+Gates, learning paths all live)

**Shipped in 0.38.0–0.42.0:**
- **0.38.0** — HQ Icon superset (84+4 icons, GLYPH_TO_ICON) + CompanyLogo (319 company mappings); 6 emoji/glyph occurrences replaced with Icon components.
- **0.39.0** — Worlds + Gates: 7-world tab layer over PyLabBrowser. Files: `pyLabWorlds.js` (7 worlds + topic mappings), `worldGates.js` (localStorage `pl-world-gates-v1`), `WorldTabs.jsx`, `WorldGate.jsx` (two-path unlock overlay), `GateQuiz.jsx` (MCQ quiz, 80% threshold, graceful fallback when < 3 MCQs). PyLabBrowser fully wired.
- **0.40.0** — Content ramp batch 1: `pyLabBatch_ramp1.js` — 5 warmup problems (col-mean, filter-rows, filter-notna, groupby-mean, groupby-count-nan) + 4 engineered fixtures. All CPython-verified; 141 problems total / 0 T1 / 0 T2 audit clean.
- **0.41.0** — SQL Lab parity layout + contrast. Two-column solve: reveal section (model solution, debrief, JudgmentLayer, ScaleRace, RefactorChallenge, FollowUpChain, ForwardPointerCard) moved into RIGHT column — left (prompt+schema) permanently fixed. Contrast: Graphite theme (`[data-skin='platinum'][data-theme='dark']`) boosted — `--text-secondary #c3c3c8→#dddde2`, `--text-muted #9a9aa0→#bbbbc2`, `--text-dim #74747a→#9a9aa0`; also bumped in light-root and explicit-light blocks. Debrief text `var(--text-secondary)→var(--text)`. Fixture preview `var(--text-muted)→var(--text)`.
- **0.42.0** — Learning paths built and wired. `src/data/pyLabPaths.js`: 16 path objects (7 worlds × 2 tiers + 2 lab-wide cross-world). PyLabBrowser: `activePath`/`activeDay` state; PathSelector (two cards above the grid when no path active); day strip (pill buttons + active day title/focus + × to exit); filter logic (problemIds if populated → day worlds-topics for lab-wide → world filter fallback); path resets on world tab switch._

## DO frame — PyLab (current, 2026-06-24)
**PyLab is the single DO bank for pandas/numpy AND Python — 136 problems, all gate-verified.** Built to the SQL-Lab bar (a judgment gym, not a grader): `solve(...)→output` graded by the typed comparator `pl_compare`; engineered fixtures; per-problem judgment layer (98 carry one — a verified runs-but-wrong trap + a method dial). Four committed gates run before commit: `audit_py.py` (Tier-1 0), `verify_py_methods.py` (non-trap≡solution, traps diverge), `py_content_scan.mjs` (de-jargon/hints/debrief), `run_py.py` (`--diverge` authoring). Runtime `runPyLab` (Pyodide). Standalone pandas/drills/idioms/oop rooms folded in (nav + `banks.js`); **Gotchas stays its own predict→reveal room**.
- **Architecture (the authority): `docs/PYLAB-VISION.md`** (D-PL-22) — two-axis ROLE×SENIORITY, 9 formats, roadmap, from market research across all seven roles. Contract spec: `docs/PYLAB-BUILD-SPEC.md`; handoff: `docs/PYLAB-HANDOFF.md`.
- **Phase 1 shipped (0.20.0):** `pyLabMeta.js` (roles[]+level, derived) + role/level filters + `PyLabReadiness` dashboard.
- **Phase 2 shipped (0.22.0–0.23.0):** **Scale-it race** (`ScaleRace.jsx`+`runPyLabBench`), **Ambiguity drill** (`AmbiguityDrill.jsx`), **Refactor** (`RefactorChallenge.jsx`+`runPyLabBenchFull`). Last two read a decoupled `src/data/pyLabFormats.js` (id→{ambiguity?,refactor?}; 8+5 authored, bank untouched). esbuild + CPython verified.
- **Phase 3 COMPLETE (0.24.0–0.32.0):** **Trap Museum** (`TrapMuseum.jsx`+`pyLabTraps.js`) — gallery of all **100 runs-but-wrong traps**, copy-as-post, JUDGE nav; **Spaced repetition** (`pyLabSR.js`+ PyLab strip) — light SM-2 (1d→3d→×ease; lapse→due now; tracks only submitted), "N due for review" review queue; **Mock-loop** (`MockLoop.jsx` + a launcher by the PyLab title) — a timed, no-reveal session → scorecard, misses feed SR; **Follow-up chains** (`FollowUpChain.jsx`+`pyLabFollowups.js`) — the interviewer's escalating next questions unfold one at a time in the reveal (28 chains / 73 follow-ups, reveal-only, CPython-verified). Trap Museum + SR are pure infra; Mock-loop reuses `runPyLab`.
- **UI / identity (0.25.0–0.30.0):** dark mode = **Graphite** (a dark theme of Platinum; green-screen retired — D-PL-23); theme-aware **editor syntax highlighting** (`--cm-*` tokens); PyLab grid → **square company cards** (`pyLabCompanies.js`; representative tags — D-PL-25) with a one-line gist, WARMUP/CORE badge dropped; **two-pane solve view** + **schema panel** (`PyLabSchema.jsx`←`pyLabSchemas.js`, build-time CPython introspection; input sample + output SHAPE ONLY — D-PL-26) + **schema-name autocomplete** + `Cmd/Ctrl+Enter`→Submit (`Prec.highest`); **cleanup** — old bank files + `ProblemBrowser` + dead green CSS removed (full import-graph bundle exit 0).
- **Beginner tutorial ladder (0.37.0, D-PL-28) — NEW:** the SQLBolt-style on-ramp *inside* PyLab — a "Start from zero" banner at the top of the bank → an internal `tutorial` view (like Mock interview, no route/nav change). `src/data/pyTutorial.js` (Python lessons **1–5 authored**, 20 inline tasks, every solution+check **CPython-verified** — correct passes, starter fails; lessons 6–18 + an 8-lesson pandas section as planned stubs) + `src/pages/PyTutorial.jsx` (the lesson player, reuses `PythonCell` + `runCheck`). Ramps **0 → Easy** then hands to the gym (`graduatesTo`). Spec: `docs/PYLAB-TUTORIAL-SPEC.md`. **Next: author Python lessons 6–13 (T1).**
- **Key files:** `src/data/{pyLabProblems,pyLabFixtures,pyLabBatch_*}.js` (the bank), `pyLabMeta.js`, `pyLabFormats.js`, `pyLabTraps.js`, `pyLabCompanies.js`, `pyLabSchemas.js`, `pyLabFollowups.js`; `src/utils/pyLabSR.js`; `src/pages/{PyLabBrowser,TrapMuseum}.jsx`; `src/components/shared/{JudgmentLayer,PyLabReadiness,ScaleRace,AmbiguityDrill,RefactorChallenge,MockLoop,PyLabSchema,FollowUpChain}.jsx`; `scripts/{pl_compare,_pylab_harness,audit_py,verify_py_methods,run_py,build_py_schemas}.py` + `_extract_pylab.mjs` + `py_content_scan.mjs`.
- **Next:** **Phase 4** — take-home · code-review · explain-it (see `docs/PYLAB-VISION.md`). Continuous: grow ambiguity/refactor/follow-up coverage. **Deploy the 0.22–0.32 stack first.**

---


## KNOW frame — Foundations (current, 2026-06-24)
**KNOW is one navigable Foundations surface** — the re-scope from a 20-card predict-run-read stub (D-PL-21), triggered by `docs/FOUNDATIONS-HANDOFF.md` (PAL's mentor note: *teach by manipulation, not exposition*) + a live read proving PAL's KNOW is slider-driven and ~10x PL's codebase. **Architecture:** a **trunk** (5 rooms: Python Foundations · The Machine · DS&A · NumPy & pandas · Shipping Python) + **2 branches** (Competitive Programming · Tensors & Autograd) that amend the charter (CP above the easy→med ceiling; Tensors = library mechanics only, modeling stays in MSL).
- **Authority spec:** `docs/FOUNDATIONS-SPEC.md` (room scope, the manipulable-model bar, the 3 through-lines, build order F0–F7). Registry: `src/data/foundationsRooms.js` (7 rooms / 24 clusters / 73 planned modules + `KNOW_BACKING`/`KNOW_EXTRA` folding the 20 authored modules in).
- **Wired (F0):** `src/pages/FoundationsBrowser.jsx` reads the registry → trunk/branches → rooms → clusters → module cards; in the KNOW nav + route. A module is READY if it has authored predict→reveal content (opens `KnowRunner`) OR a driven model (opens a lightweight widget runner).
- **KNOW merged (D-PL-21):** the 20 "Python & OOP Depth" modules fold INTO Foundations (`KNOW_BACKING`/`KNOW_EXTRA` → 17 Python Foundations / 2 Shipping / 1 Machine); standalone nav item removed → one KNOW surface.
- **19 driven models, all 7 rooms covered.** **Two config templates (D-PL-24):** `StateTrace.jsx` (+ `foundationsModels.js`, binding/identity ×4: aliasing, copy-view, mutable-default, is-vs-==) and `ArrayTrace.jsx` (+ `arrayTraceModels.js`, DSA traces ×3: two-pointer, sliding-window, binary-search) — a new such model is DATA, not a component. **Bespoke** (custom viz): call-stack, Big-O, hashing, vectorized-race (live numpy), broadcasting, index-align (live pandas), truthiness, decorators, generators, async-timeline, DP grid (Room 6), autograd (Room 7). `KnowRunner` gained an optional `interactive` slot (`interactiveModules.js` maps id→widget). Live models run real CPython/pandas/numpy in Pyodide; all CPython/esbuild verified. Themes under Graphite (var tokens).
- **Key files:** `docs/FOUNDATIONS-SPEC.md`; `src/data/foundationsRooms.js`; `src/pages/FoundationsBrowser.jsx`; `src/components/foundations/` (`StateTrace`+`foundationsModels`, `ArrayTrace`+`arrayTraceModels`, `interactiveModules`, + the bespoke widget files); `KnowBrowser.jsx` (`KnowRunner` exported + slot). Decisions: **D-PL-21** (the rooms), **D-PL-24** (the template discipline).
- **Active "your turn" step (0.31.0):** `KnowRunner` has an optional do-and-check step (`m.yourTurn` + `runCheck` in `pyodideRuntime.js`) — edit code to a goal → Check / Cmd+Enter → **targeted** feedback (never just "wrong"). The deepest SQL-Lab lesson for KNOW: *active recall* — the pedagogy, not the solve-chrome (no schema browser / company tags; Foundations isn't a problem bank). Rolled out to **8 read-run modules** (closure, aliasing, or-default, mutable-default, eq/hash, wraps, iterator protocol, operator dispatch), all CPython-verified. Editor autocomplete + Cmd+Enter came free from the PyLab editor work (D-PL-26); the your-turn cell reuses `onSubmit`. Next: extend `yourTurn` to more read-run modules (authoring, no new engineering).
- **Next (F2/F3):** mostly **authoring**, not engineering — write the planned modules' predict→read text alongside the widgets; migrate more repeating shapes onto the templates; `git rm` the 5 superseded bespoke files (Aliasing/CopyVsView/TwoPointer/SlidingWindow/BinarySearch — sandbox couldn't delete). All Foundations build pushed approve-first (macOS build); deployed incrementally.

## What PL is
Programming Lab — the SWE-for-data fluency lab (D-07). React + Vite + Pyodide SPA, sibling-consistent with PAL/MSL/GAL. Repo slug still `production-systems-lab` (rename deferred — NEXT.md).

## Built (this session)
- **B0 foundation:** legacy FastAPI/Docker/`modules` infra archived to `_legacy/`. New SPA scaffold (package.json/vite/index.html matching PAL's React 19 + Vite 8 + Tailwind v4 stack). Theme `index.css` = PAL tokens + `.pal-*` animation classes verbatim.
- **MVP IDE + glass-box:** `PythonCell` (CodeMirror 6 editor → Pyodide 0.25.1 from jsdelivr, main-thread per MSL) runs real Python in-browser. Glass-box layer built in now (not last): `runPythonGlassBox` captures stdout + wall time (`perf_counter`) + peak memory (`tracemalloc`); footer renders "⏱ ms · 🧠 KB peak". `raceMethods()` helper ready for the future DSA canonical-vs-brute race.
- **Inherited components:** Sidebar (KNOW/DO/BUILD/JUDGE, D-15), Icon, HowToStrip, ForwardPointerCard, synthesized GateOverlay (passthrough in beta), unlock.js (`isUnlocked()`=true).
- **B1 Bank A — Python Gotchas:** 23 problems across 7 clusters (identity-mutation 5, truthiness 4, evaluation-timing 3, memory-cost 4, numbers 3, scope-names 3, syntax 1). Each: predict-MCQ → runnable code → glass-box reveal → runnable fix → "Copy as LinkedIn post". Seeded by PY1–PY7; 16 newly authored. Every Python snippet verified against CPython.
- **Pages:** `GotchaBrowser` (DO-room, cluster-grouped grid, mobile-safe) + `GotchaRunner` (the full flow). `App` routes home ↔ gotchas with lazy-load + Suspense.

## Verified
- `node --check` + esbuild bundle of the whole import graph → exit 0 (99.4kb JS / 14.8kb CSS). Data file: 23 unique ids, integrity clean, balanced delimiters, all clusters covered.
- Every gotcha `code` + `fix` output verified in CPython 3.10; outputs chosen to be stable on Pyodide's 3.11.
- **Build verified on macOS (2026-06-23):** `vite 8.0.16`, 44 modules, `✓ built in 217ms` — dist JS 243kB (gzip 77kB) + lazy GotchaBrowser/CodeMirror chunk 372kB (gzip 124kB) + CSS 20kB. Committed `a7677fc`, pushed to `origin/main`.

## Known deviations / debt
- **Pyodide runs on the main thread** (adopted from MSL verbatim), not a Web Worker as the spec/NEXT suggested. Fine for short gotcha snippets; long-running DSA/pandas later should move to a worker. Flagged for a hardening pass.
- **Repo slug renamed → `programming-lab`** (GitHub repo renamed, local remote repointed via `git remote set-url`, 2026-06-23). **Local folder is still `labs/production-systems-lab`** — left as-is so the Cowork mount + BreakLabs/CLAUDE.md paths keep working; rename the local dir in a later infra pass if desired. Remote: `github.com/SidharthKriplani/programming-lab`.
- Cost-gotchas (#13–16) use small runnable `n` for speed; the scale lesson lives in the text + post (the glass-box footer still shows the relative cost).

## Identity (2026-06-23)
**Instrument** theme — dark-first, **violet `#8B5CF6`** on void `#0A0A0B`, Inter + JetBrains Mono; gold = signal, red = break, green = fix (BreakLabs master palette). Light "Field Notes" mode via `[data-theme]` toggle (sidebar footer), `localStorage`-persisted, anti-flash inline script. break-glyph favicon + `break⌇labs` wordmark. Pure token swap in `index.css` (no component rewrites). Supersedes the inherited PAL light/indigo skin.

**BreakLabs logo (D-19, PL 0.3.0):** `BrandMark` component (`break⌇labs · Programming`, red seam constant + violet descriptor) wired into 7 slots — sidebar header, favicon (shared red monogram), OG card (`public/og-image.png`), hero, gate, footer, loading. Per `docs/BRANDMARK-ROLLOUT.md`.

## Deploy
GitHub: `github.com/SidharthKriplani/programming-lab` (pushed `a7677fc`, build verified locally). **Vercel import pending** — first deploy via dashboard (Add New → Project → import `programming-lab`, Vite auto-detected). After import, every push to `main` auto-deploys.

## Gate
Distribution-gate override is conscious + conditioned (NEXT.md): PL build proceeds only because every Bank-A gotcha doubles as a LinkedIn post. If the daily slips, PL pauses.
